# ARATA　Tools
A tool for creating training data for ARATA, software that classifies pixels representing roots in soil images
#　How to use
When you run `Image_crop.py` in Python, it crops the files in the Input directory to a size of 400x400 pixels.

When you run `Learning_conversion.py`, it changes the white pixels in the images contained in the input file to red and the 1-pixel outlines to 